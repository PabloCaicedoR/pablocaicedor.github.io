---
title: "React, TypeScript y Vite desde cero"
subtitle: "Del warning de VS Code a una aplicación biomédica tipada"
author: "BioForja"
image: "../recursos/imagenes/tutoriales/react-typescript-vite.png"
date: "2026-08-04"
lang: es
toc: true
toc-depth: 3
number-sections: true
code-copy: true
code-overflow: wrap
smooth-scroll: true
link-external-newwindow: true
format:
  html:
    page-layout: article
    code-fold: false
    code-line-numbers: true
execute:
  enabled: false
---

## Propósito y resultado

Este tutorial resuelve el warning que aparece en `settings.json` y usa el problema como punto de entrada para aprender **TypeScript, React y Vite**. Al finalizar podrás:

- comprender por qué VS Code subraya determinadas preferencias;
- distinguir JavaScript, TypeScript, React y Vite;
- crear un proyecto React con TypeScript;
- modelar datos biomédicos mediante tipos e interfaces;
- construir componentes, propiedades, estado y eventos;
- ejecutar pruebas, aplicar lint y generar una compilación de producción;
- incorporar esta página `.qmd` a un sitio Quarto sin reemplazar su tema.

::: {.callout-note title="Una precisión importante"}
TypeScript sí es un **lenguaje de programación**. React es una **biblioteca para construir interfaces** y Vite es una **herramienta de desarrollo y compilación**. Los tres se complementan, pero no cumplen la misma función.
:::

## Diagnóstico del warning de VS Code

### Qué muestran las líneas subrayadas

La configuración contiene estas cuatro preferencias:

```jsonc
"typescript.updateImportsOnFileMove.enabled": "prompt",
"javascript.updateImportsOnFileMove.enabled": "prompt",
"typescript.preferences.importModuleSpecifier": "shortest",
"javascript.preferences.importModuleSpecifier": "shortest"
```

Su sintaxis es válida y los valores también lo son. El subrayado naranja no significa que el archivo JSON esté roto. Indica que las claves se consideran **obsoletas** en las versiones actuales de VS Code.

Desde VS Code 1.110, publicado en febrero de 2026, las configuraciones separadas `javascript.*` y `typescript.*` se están reemplazando por configuraciones unificadas `js/ts.*`. Las opciones antiguas todavía funcionan, pero VS Code las marca como *deprecated* y una opción unificada tiene prioridad cuando ambas existen. Consulta las [notas oficiales de VS Code 1.110](https://code.visualstudio.com/updates/v1_110#_unified-javascript-and-typescript-settings).

### Corrección recomendada

Elimina las cuatro claves antiguas y usa solamente estas dos:

```jsonc
// React, JavaScript y TypeScript
"js/ts.updateImportsOnFileMove.enabled": "prompt",
"js/ts.preferences.importModuleSpecifier": "shortest",
"csharp.semanticHighlighting.enabled": true
```

La equivalencia es exacta:

| Configuración antigua | Configuración actual | Función |
|---|---|---|
| `javascript.updateImportsOnFileMove.enabled` | `js/ts.updateImportsOnFileMove.enabled` | Pregunta si debe actualizar imports al mover un archivo |
| `typescript.updateImportsOnFileMove.enabled` | `js/ts.updateImportsOnFileMove.enabled` | La misma regla, ahora compartida |
| `javascript.preferences.importModuleSpecifier` | `js/ts.preferences.importModuleSpecifier` | Define el estilo de rutas para imports automáticos |
| `typescript.preferences.importModuleSpecifier` | `js/ts.preferences.importModuleSpecifier` | La misma preferencia, ahora compartida |

La configuración `"prompt"` conserva el comportamiento equilibrado: VS Code no modifica imports sin avisar. El valor `"shortest"` elige el import disponible con la ruta más corta. Ambas claves actuales están registradas por la extensión integrada de TypeScript de VS Code en su [manifiesto oficial](https://github.com/microsoft/vscode/blob/main/extensions/typescript-language-features/package.json).

### Aplicar y verificar la solución

1. Abre `settings.json` con **Preferences: Open User Settings (JSON)**.
2. Reemplaza las cuatro claves antiguas por las dos claves unificadas.
3. Guarda el archivo.
4. Ejecuta **Developer: Reload Window** desde la paleta de comandos.
5. Coloca el cursor sobre las claves: no debe aparecer el mensaje de obsolescencia.
6. Crea temporalmente un archivo importado, muévelo en el explorador y comprueba que VS Code pregunta si debe actualizar sus imports.

::: {.callout-tip title="Cómo comprobar una configuración sin adivinar"}
Ejecuta **Preferences: Open Default Settings (JSON)** y busca el nombre de la clave. Esa vista muestra las preferencias que la versión instalada de VS Code reconoce. La [referencia oficial de configuraciones](https://code.visualstudio.com/docs/reference/default-settings) describe este procedimiento.
:::

### Si el mensaje dice “Unknown Configuration Setting”

Ese sería un problema distinto. Las dos causas más comunes son:

1. **La extensión integrada está deshabilitada.** En Extensiones (`Ctrl+Shift+X`), escribe `@builtin typescript`, abre **TypeScript and JavaScript Language Features** y selecciona **Enable**.
2. **La opción está en un bloque incorrecto.** Las claves deben estar en el nivel principal de `settings.json`, no dentro de `workbench.colorCustomizations` ni dentro de otro objeto que no admita esa preferencia.

Después ejecuta **Developer: Reload Window**. Las extensiones integradas no se desinstalan; pueden deshabilitarse y volver a habilitarse, como explica la [documentación de JavaScript en VS Code](https://code.visualstudio.com/docs/nodejs/working-with-javascript#_javascript-projects-jsconfigjson).

## El mapa mental del ecosistema

Antes de escribir código conviene separar las capas:

| Tecnología | Qué es | Qué aporta | Qué no hace por sí sola |
|---|---|---|---|
| JavaScript | Lenguaje ejecutado por el navegador | Lógica, eventos y acceso a APIs web | No verifica tipos estáticos |
| TypeScript | JavaScript con un sistema de tipos | Detecta inconsistencias antes de ejecutar | El navegador no ejecuta TypeScript directamente |
| React | Biblioteca de interfaz | Componentes, estado y renderizado declarativo | No instala ni compila el proyecto |
| Vite | Herramienta de desarrollo y build | Servidor rápido, transformación y producción | No define la interfaz ni el modelo de datos |
| JSX/TSX | Sintaxis para describir una interfaz | Permite combinar marcado y lógica de componentes | No es HTML literal ni un lenguaje independiente |

```{mermaid}
flowchart TD
  A["Archivos .ts y .tsx"] --> B["TypeScript verifica tipos"]
  B --> C["Vite transforma y sirve"]
  C --> D["El navegador ejecuta JavaScript"]
  D --> E["React renderiza la interfaz"]
```

La idea esencial es esta: escribimos componentes React en archivos `.tsx`; TypeScript comprueba el contrato de los datos; Vite transforma el proyecto y entrega JavaScript al navegador.

## Preparar el entorno

### Requisitos

Necesitas:

- VS Code actualizado;
- la extensión integrada **TypeScript and JavaScript Language Features** habilitada;
- Node.js compatible;
- npm, incluido con Node.js.

Comprueba las versiones:

```bash
node --version
npm --version
```

La versión actual de Vite exige Node.js `20.19+` o `22.12+`; una versión mayor compatible también sirve. Verifica siempre el requisito vigente en la [guía oficial de Vite](https://vite.dev/guide/#scaffolding-your-first-vite-project).

::: {.callout-warning title="Evita instalaciones globales innecesarias"}
No necesitas `sudo npm install -g vite`. El generador `npm create vite@latest` y las dependencias locales del proyecto funcionan sin privilegios de administrador.
:::

### Extensiones recomendadas en VS Code

Instala únicamente lo que aporta una función clara:

| Extensión | Identificador | Uso |
|---|---|---|
| Oxc | `oxc.oxc-vscode` | Integra Oxlint, incluido en la plantilla actual de Vite |
| Prettier | `esbenp.prettier-vscode` | Formatea TS, TSX, JSON, CSS y Markdown |
| Error Lens, opcional | `usernamehw.errorlens` | Hace más visibles los diagnósticos en línea |

No instales una extensión adicional llamada “TypeScript”: VS Code ya incluye el soporte oficial. La extensión ESLint (`dbaeumer.vscode-eslint`) sigue siendo útil en proyectos que realmente tengan ESLint como dependencia y configuración, pero no es necesaria para la plantilla `react-ts` actual.

## Crear el primer proyecto

React recomienda actualmente usar un framework para aplicaciones completas y Vite cuando se desea aprender React desde una configuración cliente sencilla. La [guía oficial de React](https://react.dev/learn/build-a-react-app-from-scratch) incluye Vite entre las herramientas apropiadas para este segundo caso.

Desde la carpeta donde guardas tus proyectos:

```bash
npm create vite@latest bio-signal-dashboard -- --template react-ts
cd bio-signal-dashboard
npm install
npm run dev
```

Abre la dirección local que muestre la terminal, normalmente `http://localhost:5173`. Para abrir el proyecto en VS Code:

```bash
code .
```

`react-ts` es la plantilla que combina React, TypeScript y Vite. No uses la plantilla `react` si el propósito es practicar tipado desde el comienzo.

### Qué creó Vite

| Ruta | Responsabilidad |
|---|---|
| `index.html` | Punto de entrada HTML de Vite |
| `src/main.tsx` | Monta la aplicación React en el DOM |
| `src/App.tsx` | Componente principal inicial |
| `src/App.css` | Estilos del componente principal |
| `package.json` | Dependencias y comandos npm |
| `tsconfig*.json` | Reglas de TypeScript |
| `vite.config.ts` | Configuración de Vite y sus plugins |
| `.oxlintrc.json` | Reglas de Oxlint para análisis estático |

::: {.callout-note title="Tres comandos que no deben confundirse"}
`npm install` instala dependencias declaradas. `npm run dev` inicia un servidor para desarrollar. `npm run build` verifica y genera archivos optimizados para publicar.
:::

## Fundamentos mínimos de JavaScript

TypeScript amplía JavaScript; no lo sustituye. Antes de React debes reconocer variables, funciones, arreglos, objetos, módulos y operaciones inmutables.

```ts
const signalName = "ECG";              // valor que no se reasigna
let selectedCount = 0;                  // valor que sí puede cambiar

const qualities = [92, 78, 88];
const accepted = qualities.filter((value) => value >= 80);
const normalized = qualities.map((value) => value / 100);

const signal = {
  id: "ECG-001",
  sampleRate: 500,
  reviewed: false,
};

const reviewedSignal = { ...signal, reviewed: true };
```

Observa dos ideas importantes para React:

- `map` y `filter` producen nuevos arreglos en lugar de modificar el original;
- `{ ...signal, reviewed: true }` crea un objeto nuevo con una propiedad actualizada.

Los módulos dividen el programa en archivos:

```ts
// quality.ts
export function qualityLabel(value: number): string {
  return value >= 80 ? "Aceptable" : "Revisar";
}

// otro archivo
import { qualityLabel } from "./quality";
```

El warning inicial está relacionado precisamente con estos imports: VS Code puede actualizar `"./quality"` si `quality.ts` cambia de nombre o ubicación.

## TypeScript: modelar antes de programar

### Inferencia y anotaciones

TypeScript suele inferir el tipo sin que lo escribas:

```ts
const modality = "ECG";   // TypeScript infiere string
const sampleRate = 500;    // TypeScript infiere number
```

Anota tipos cuando expresan un contrato útil, especialmente en parámetros y retornos:

```ts
function durationSeconds(samples: number, sampleRate: number): number {
  return samples / sampleRate;
}
```

Esto se detecta antes de abrir la aplicación:

```ts
durationSeconds("5000", 500);
// Error: string no puede usarse donde se espera number.
```

### Uniones: estados válidos y finitos

Una unión limita un valor a alternativas conocidas:

```ts
export type SignalKind = "ECG" | "EMG" | "EEG";
export type ReviewState = "pending" | "approved" | "rejected";
```

`SignalKind` evita variantes accidentales como `"E.C.G"` o `"ecg"`. La [documentación de TypeScript](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#union-types) explica cómo las uniones representan valores con varias alternativas permitidas.

### Interfaces: el contrato de los datos

Crea `src/types.ts`:

```ts
export type SignalKind = "ECG" | "EMG" | "EEG";

export interface SignalRecord {
  id: string;
  participantCode: string;
  kind: SignalKind;
  sampleRate: number;
  quality: number;
  reviewed: boolean;
  notes?: string;
}
```

La propiedad `notes?` es opcional. Una interfaz describe la forma que debe tener cada registro. TypeScript no valida por sí solo datos externos en tiempo de ejecución; si los registros provienen de una API o archivo, todavía debes validarlos al ingresar al sistema.

### Evitar `any`

`any` desactiva la protección de TypeScript. Para datos externos aún no validados, usa `unknown`:

```ts
function parseExternalData(value: unknown): SignalRecord {
  if (typeof value !== "object" || value === null) {
    throw new Error("El registro no es un objeto válido");
  }

  // En una aplicación real continuaríamos validando cada propiedad.
  throw new Error("Validación todavía no implementada");
}
```

La frontera correcta es: **datos externos → validación → tipos internos confiables**.

## React: construir la interfaz con componentes

### JSX y TSX

Un componente React es una función que devuelve una descripción de interfaz:

```tsx
function Header() {
  return (
    <header>
      <h1>BioSignal Dashboard</h1>
      <p>Control de calidad de señales biomédicas</p>
    </header>
  );
}
```

El archivo usa `.tsx`, no `.ts`, porque contiene JSX. En JSX:

- `className` reemplaza el atributo HTML `class`;
- las expresiones JavaScript se escriben entre `{}`;
- un componente debe empezar con mayúscula;
- cada bloque retornado necesita una raíz común.

### Props: datos que entran al componente

Crea `src/components/SignalCard.tsx`:

```tsx
import type { SignalRecord } from "../types";

interface SignalCardProps {
  signal: SignalRecord;
  onToggleReviewed: (id: string) => void;
}

export function SignalCard({
  signal,
  onToggleReviewed,
}: SignalCardProps) {
  const qualityClass = signal.quality >= 80 ? "quality-ok" : "quality-warning";

  return (
    <article className="signal-card">
      <div>
        <span className="signal-kind">{signal.kind}</span>
        <h2>{signal.id}</h2>
        <p>Participante: {signal.participantCode}</p>
        <p>Frecuencia de muestreo: {signal.sampleRate} Hz</p>
      </div>

      <div className="signal-status">
        <strong className={qualityClass}>Calidad: {signal.quality}%</strong>
        <button
          type="button"
          onClick={() => onToggleReviewed(signal.id)}
        >
          {signal.reviewed ? "Marcar pendiente" : "Marcar revisada"}
        </button>
      </div>
    </article>
  );
}
```

`SignalCardProps` es el contrato del componente. Si quien lo usa omite `signal`, entrega otro tipo o define una función incompatible, TypeScript lo detecta.

`import type` indica que `SignalRecord` se necesita solo durante la verificación y no debe convertirse en un import de JavaScript en producción.

### Estado: información que cambia

React usa *Hooks* para incorporar capacidades a un componente. `useState` permite recordar información entre renderizados. Los Hooks se llaman en el nivel superior del componente, nunca dentro de un `if`, ciclo o función anidada. Consulta las [reglas y Hooks integrados de React](https://react.dev/reference/react/hooks).

Reemplaza `src/App.tsx` por lo siguiente:

```tsx
import { useState, type ChangeEvent } from "react";
import "./App.css";
import { SignalCard } from "./components/SignalCard";
import type { SignalKind, SignalRecord } from "./types";

type Filter = "ALL" | SignalKind;

const initialSignals: SignalRecord[] = [
  {
    id: "ECG-001",
    participantCode: "P-014",
    kind: "ECG",
    sampleRate: 500,
    quality: 94,
    reviewed: true,
  },
  {
    id: "EMG-002",
    participantCode: "P-021",
    kind: "EMG",
    sampleRate: 1000,
    quality: 76,
    reviewed: false,
  },
  {
    id: "EEG-003",
    participantCode: "P-008",
    kind: "EEG",
    sampleRate: 256,
    quality: 87,
    reviewed: false,
  },
];

function isFilter(value: string): value is Filter {
  return value === "ALL" || value === "ECG" || value === "EMG" || value === "EEG";
}

export default function App() {
  const [signals, setSignals] = useState<SignalRecord[]>(initialSignals);
  const [filter, setFilter] = useState<Filter>("ALL");

  const visibleSignals = signals.filter(
    (signal) => filter === "ALL" || signal.kind === filter,
  );

  const averageQuality = visibleSignals.length
    ? Math.round(
        visibleSignals.reduce((sum, signal) => sum + signal.quality, 0) /
          visibleSignals.length,
      )
    : null;

  function handleFilterChange(event: ChangeEvent<HTMLSelectElement>) {
    const value = event.target.value;
    if (isFilter(value)) {
      setFilter(value);
    }
  }

  function toggleReviewed(id: string) {
    setSignals((currentSignals) =>
      currentSignals.map((signal) =>
        signal.id === id
          ? { ...signal, reviewed: !signal.reviewed }
          : signal,
      ),
    );
  }

  return (
    <main className="app-shell">
      <header className="hero">
        <p className="eyebrow">BioForja · Biomedical AI</p>
        <h1>BioSignal Dashboard</h1>
        <p>Revisión trazable de calidad para ECG, EMG y EEG.</p>
      </header>

      <section className="toolbar" aria-label="Controles del tablero">
        <label htmlFor="kind-filter">Modalidad</label>
        <select
          id="kind-filter"
          value={filter}
          onChange={handleFilterChange}
        >
          <option value="ALL">Todas</option>
          <option value="ECG">ECG</option>
          <option value="EMG">EMG</option>
          <option value="EEG">EEG</option>
        </select>

        <output>
          Calidad promedio: {averageQuality === null ? "Sin datos" : `${averageQuality}%`}
        </output>
      </section>

      <section className="signal-list" aria-live="polite">
        {visibleSignals.map((signal) => (
          <SignalCard
            key={signal.id}
            signal={signal}
            onToggleReviewed={toggleReviewed}
          />
        ))}
      </section>
    </main>
  );
}
```

### Qué estás aprendiendo en `App.tsx`

| Expresión | Concepto |
|---|---|
| `useState<SignalRecord[]>` | Estado genérico: solo acepta un arreglo de registros válidos |
| `type Filter = "ALL" \| SignalKind` | Composición de uniones |
| `value is Filter` | Guardia de tipos y estrechamiento seguro |
| `.filter(...)` | Dato derivado; no necesita otro estado |
| `.reduce(...)` | Agregación de datos |
| `setSignals(current => ...)` | Actualización basada en el estado anterior |
| `{ ...signal, reviewed: ... }` | Actualización inmutable de un objeto |
| `key={signal.id}` | Identidad estable de cada componente en una lista |
| `onChange` y `onClick` | Manejo declarativo de eventos |

No guardamos `visibleSignals` ni `averageQuality` en estado porque ambos se calculan a partir de `signals` y `filter`. Duplicar estado obliga a sincronizar copias y crea errores evitables; React recomienda [evitar estado redundante](https://react.dev/learn/choosing-the-state-structure).

React considera el estado de objetos y arreglos como solo lectura: se reemplaza con una nueva estructura en lugar de mutarse. Este patrón está documentado en [`useState`](https://react.dev/reference/react/useState#updating-objects-and-arrays-in-state).

### Añadir estilos

Reemplaza `src/App.css`:

```css
:root {
  color: #d9e3de;
  background: #101416;
  font-family: Inter, system-ui, sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
}

button,
select {
  font: inherit;
}

.app-shell {
  width: min(960px, 92vw);
  margin: 0 auto;
  padding: 3rem 0;
}

.hero {
  padding: 2rem;
  border: 1px solid #375547;
  border-radius: 1rem;
  background: #17201c;
}

.hero h1 {
  margin: 0.25rem 0;
}

.eyebrow,
.signal-kind {
  color: #8fb9a2;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.toolbar {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.75rem;
  margin: 1.5rem 0;
}

.toolbar output {
  margin-left: auto;
}

select,
button {
  color: #e6eee9;
  border: 1px solid #5f8f72;
  border-radius: 0.5rem;
  background: #1b2923;
  padding: 0.6rem 0.8rem;
}

button {
  cursor: pointer;
}

button:hover,
button:focus-visible,
select:focus-visible {
  outline: 2px solid #9bc6ad;
  outline-offset: 2px;
}

.signal-list {
  display: grid;
  gap: 1rem;
}

.signal-card {
  display: flex;
  justify-content: space-between;
  gap: 1.5rem;
  padding: 1.25rem;
  border: 1px solid #30443b;
  border-radius: 0.85rem;
  background: #151b1d;
}

.signal-card h2 {
  margin: 0.25rem 0;
}

.signal-status {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: space-between;
  gap: 1rem;
}

.quality-ok {
  color: #95c9a9;
}

.quality-warning {
  color: #e4b875;
}

@media (max-width: 640px) {
  .signal-card {
    flex-direction: column;
  }

  .signal-status {
    align-items: stretch;
  }

  .toolbar output {
    width: 100%;
    margin-left: 0;
  }
}
```

El diseño usa HTML semántico, `label`, `output`, estados de foco visibles y una adaptación para pantallas estrechas. React no reemplaza las buenas prácticas de HTML y CSS; las organiza en componentes.

## Cómo trabaja Vite

### Desarrollo y actualización rápida

```bash
npm run dev
```

Vite inicia un servidor local y actualiza los módulos modificados sin recargar toda la aplicación. Este mecanismo permite ver cambios de componentes y estilos casi inmediatamente.

### Compilación de producción

```bash
npm run build
```

La plantilla `react-ts` ejecuta la verificación de TypeScript y luego genera la versión optimizada en `dist/`. Vite usa `index.html` como entrada y produce recursos aptos para alojamiento estático, según su [guía de compilación](https://vite.dev/guide/build.html).

Prueba localmente esa versión:

```bash
npm run preview
```

`preview` sirve para revisar el resultado compilado; no es el servidor que debe usarse como infraestructura de producción.

### Variables de entorno

Crea `.env.local`:

```dotenv
VITE_API_BASE_URL=http://localhost:8000
```

Úsala con tipo `string`:

```ts
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL;
```

::: {.callout-important title="No guardes secretos en variables VITE_"}
Toda variable con prefijo `VITE_` queda expuesta al código cliente y puede inspeccionarse en el navegador. Nunca pongas allí contraseñas, llaves privadas ni tokens sensibles. Consulta la [documentación oficial de variables de entorno](https://vite.dev/guide/env-and-mode.html).
:::

## Calidad de código en VS Code

### Configuración moderna para el proyecto

Puedes guardar preferencias compartidas en `.vscode/settings.json` dentro del proyecto:

```jsonc
{
  "editor.formatOnSave": true,
  "js/ts.updateImportsOnFileMove.enabled": "prompt",
  "js/ts.preferences.importModuleSpecifier": "shortest",

  "[javascript][javascriptreact][typescript][typescriptreact]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode",
    "editor.codeActionsOnSave": {
      "source.organizeImports": "explicit",
      "source.fixAll.oxc": "explicit"
    }
  }
}
```

Esta configuración mantiene confirmación al mover archivos, formatea al guardar y aplica correcciones disponibles de Oxlint. La extensión oficial Oxc reconoce `source.fixAll.oxc`. Si el equipo no quiere compartir preferencias personales, conserva estas claves en la configuración de usuario y no confirmes `.vscode/settings.json` en Git.

### Oxlint

La plantilla React + TypeScript actual de Vite 8 incluye Oxlint y un archivo `.oxlintrc.json`. Ejecuta:

```bash
npm run lint
```

Oxlint analiza patrones problemáticos; TypeScript comprueba contratos de tipos; Prettier decide formato. Son responsabilidades distintas. El [`package.json` oficial de la plantilla](https://github.com/vitejs/vite/blob/main/packages/create-vite/template-react-ts/package.json) permite verificar qué herramienta incluye la versión actual.

::: {.callout-note title="Si tu plantilla todavía usa ESLint"}
No migres solo por seguir el tutorial. Conserva `dbaeumer.vscode-eslint` y `"source.fixAll.eslint": "explicit"` mientras `package.json` ejecute ESLint. La configuración del editor debe corresponder a las dependencias reales del proyecto.
:::

### Prettier

Instálalo localmente para que el proyecto no dependa solo de la extensión de VS Code:

```bash
npm install --save-dev prettier
```

Agrega estos comandos a la sección `scripts` de `package.json` sin borrar los existentes:

```json
{
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
```

Ejecuta:

```bash
npm run format
npm run format:check
```

## Pruebas con Vitest

Para practicar pruebas sin introducir todavía un navegador simulado, extrae una función pura.

Crea `src/utils/quality.ts`:

```ts
export type QualityLevel = "high" | "acceptable" | "review";

export function classifyQuality(value: number): QualityLevel {
  if (value < 0 || value > 100) {
    throw new RangeError("La calidad debe estar entre 0 y 100");
  }

  if (value >= 90) return "high";
  if (value >= 80) return "acceptable";
  return "review";
}
```

Instala Vitest:

```bash
npm install --save-dev vitest
```

Agrega los comandos:

```json
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run"
  }
}
```

Crea `src/utils/quality.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { classifyQuality } from "./quality";

describe("classifyQuality", () => {
  it("clasifica los límites definidos", () => {
    expect(classifyQuality(90)).toBe("high");
    expect(classifyQuality(80)).toBe("acceptable");
    expect(classifyQuality(79)).toBe("review");
  });

  it("rechaza porcentajes imposibles", () => {
    expect(() => classifyQuality(101)).toThrow(RangeError);
  });
});
```

Durante el desarrollo:

```bash
npm test
```

Para una ejecución única, adecuada para integración continua:

```bash
npm run test:run
```

Vitest comparte el ecosistema y la transformación de Vite, por lo que resulta natural para este proyecto. Consulta su [guía oficial](https://vitest.dev/guide/).

## Flujo de trabajo diario

Un ciclo corto y verificable es más útil que acumular cambios:

```{mermaid}
flowchart TD
  A["Definir el tipo"] --> B["Construir un componente"]
  B --> C["Probar en desarrollo"]
  C --> D["Ejecutar lint y tests"]
  D --> E["Generar build"]
  E --> A
```

Usa estos comandos desde la raíz:

```bash
npm run dev
npm run lint
npm run test:run
npm run build
```

Antes de un commit, los tres últimos deben terminar sin errores.

## Errores frecuentes y cómo razonarlos

### “Cannot find module…”

Comprueba, en este orden:

1. que el archivo exista;
2. que la ruta relativa sea correcta;
3. que las mayúsculas coincidan exactamente en Linux;
4. que hayas ejecutado `npm install`;
5. que el símbolo se exporte y se importe con el mismo tipo de exportación.

```ts
// Exportación nombrada
export function SignalCard() {}
import { SignalCard } from "./SignalCard";

// Exportación por defecto
export default function App() {}
import App from "./App";
```

### “Type ... is not assignable to type ...”

No conviertas inmediatamente con `as` ni uses `any`. Lee el tipo esperado y el recibido. Corrige el modelo, valida la entrada o aplica una guarda de tipos, como `isFilter`.

### La interfaz no cambia después de modificar un objeto

Evita mutar estado:

```ts
// Incorrecto
signal.reviewed = true;

// Correcto
const updated = { ...signal, reviewed: true };
```

### Un Hook se ejecuta condicionalmente

Esto es incorrecto:

```tsx
if (visible) {
  const [filter, setFilter] = useState("ALL");
}
```

Los Hooks deben ejecutarse siempre en el mismo orden. Decláralos al comienzo del componente.

### La página queda en blanco

Revisa:

- la consola del navegador (`F12`);
- la terminal donde corre Vite;
- que `src/main.tsx` monte `<App />` en el elemento `#root`;
- que no hayas borrado `<div id="root"></div>` de `index.html`.

## Integración con un sitio Quarto

Este tutorial está diseñado como una **página**, no como un proyecto Quarto independiente. Por eso no incluye un `_quarto.yml` propio ni fija un tema: al copiar el `.qmd` dentro de tu sitio, hereda navegación, tema, tipografías y recursos globales.

Un flujo seguro es:

1. Copia `tutorial-react-typescript-vite.qmd` a una carpeta como `tutoriales/`.
2. Agrega una entrada a la navegación existente; no reemplaces el bloque completo.
3. Desde la raíz del sitio ejecuta `quarto preview`.
4. Comprueba vínculos, tabla de contenido, diagramas Mermaid y bloques de código.

Ejemplo mínimo para **integrar** en una navegación existente:

```yaml
website:
  navbar:
    left:
      - href: tutoriales/tutorial-react-typescript-vite.qmd
        text: React, TypeScript y Vite
```

::: {.callout-warning title="Quarto y Vite generan productos distintos"}
Quarto renderiza este material educativo. Vite compila la aplicación React. No copies `node_modules` al sitio ni intentes hacer que Quarto lo renderice. Si publicas la aplicación, usa el contenido de `dist/` en una ruta separada o un despliegue independiente.
:::

## Ejercicios progresivos

### Nivel 1: reforzar TypeScript

1. Agrega la modalidad `PPG` a `SignalKind` y al filtro.
2. Convierte `quality` en un tipo que documente que representa un porcentaje.
3. Añade `durationSeconds?: number` a `SignalRecord`.
4. Intenta asignar una frecuencia de muestreo como texto y explica el diagnóstico.

### Nivel 2: reforzar React

1. Muestra cuántas señales están pendientes.
2. Agrega un botón “Revisar todas”.
3. Extrae `FilterControls` como componente con props tipadas.
4. Muestra un mensaje semántico cuando un filtro no tenga resultados.

### Nivel 3: calidad y arquitectura

1. Usa `classifyQuality` dentro de `SignalCard`.
2. Añade pruebas para `0`, `80`, `90` y `100`.
3. Carga datos desde un archivo JSON y valida cada registro antes de usarlo.
4. Separa acceso a datos, reglas de dominio y componentes visuales.

### Nivel 4: conexión con una API

1. Crea una API local que responda registros sin información identificable.
2. Define estados explícitos: `idle`, `loading`, `success` y `error`.
3. Implementa cancelación de solicitudes cuando el componente deje de usarse.
4. Muestra mensajes comprensibles sin revelar detalles internos del servidor.

## Lista de verificación final

- [ ] Reemplacé cuatro claves obsoletas por dos claves `js/ts.*`.
- [ ] El warning desapareció después de recargar VS Code.
- [ ] Puedo explicar la diferencia entre TypeScript, React y Vite.
- [ ] El proyecto inicia con `npm run dev`.
- [ ] Los datos de señales tienen un contrato `SignalRecord`.
- [ ] Los componentes reciben props tipadas.
- [ ] El estado se actualiza sin mutaciones.
- [ ] `npm run lint` con la herramienta declarada por el proyecto finaliza correctamente.
- [ ] `npm run test:run` finaliza correctamente.
- [ ] `npm run build` genera `dist/` sin errores.
- [ ] La página `.qmd` se integra al sitio sin definir otro proyecto Quarto.

## Referencias oficiales

- [VS Code 1.110: configuraciones unificadas de JavaScript y TypeScript](https://code.visualstudio.com/updates/v1_110#_unified-javascript-and-typescript-settings)
- [JavaScript en VS Code](https://code.visualstudio.com/docs/languages/javascript)
- [TypeScript en VS Code](https://code.visualstudio.com/docs/languages/typescript)
- [Manual de TypeScript](https://www.typescriptlang.org/docs/handbook/intro.html)
- [Inicio rápido de React](https://react.dev/learn)
- [Construir una aplicación React desde cero](https://react.dev/learn/build-a-react-app-from-scratch)
- [Guía de Vite](https://vite.dev/guide/)
- [Compilación para producción con Vite](https://vite.dev/guide/build.html)
- [Plantilla oficial React + TypeScript de Vite](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts)
- [Integración de Oxlint con editores](https://oxc.rs/docs/guide/usage/linter/editors.html)
- [Guía de Vitest](https://vitest.dev/guide/)
